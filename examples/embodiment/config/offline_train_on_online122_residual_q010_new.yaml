# Offline TD3+BC training on online trajectory data
# Source: 20260620-14:59:15-realworld_piper_peginsertion_td3_rl_token_online
# Experiment: residual actor, q_weight=0.1, bc_weight=50
#
# Run like other embodiment examples:
#   bash examples/embodiment/run_offline_train_openrlt.sh offline_train_on_online122_residual_q010_new

# --- Path roots (data_dir, feature_cache, output_dir derived automatically) ---
online_log_dir: /home/focal/shared_disk/users/kwj/RLinf/logs/20260620-14:59:15-realworld_piper_peginsertion_td3_rl_token_online # need change
output_name: offline_train_on_online122_residual_q010_new

# --- Fixed paths ---
norm_stats_path: /home/focal/shared_disk/users/kwj/weight/openpi/pi05_cube_insert/norm_stats.json # need change
urdf_path: /home/focal/shared_disk/users/kwj/RLinf/assets/piper_local_assets/piper.urdf # need change

# --- Training schedule ---
max_steps: 1500
batch_size: 256
gpu: 0
save_interval: 50
log_interval: 10
patience: 700
viz_per_class: 5

# --- RL hyper-parameters ---
gamma: 0.94
n_step: 10
bc_weight: 50.0
q_weight: 0.1
q_warmup_steps: 0
delta_weight: 10.0
joint_abs_weight: 100.0
tcp_weight: 5000.0
tcp_boundary_weight: 1000.0

# --- Actor ---
actor_residual_ref: true
actor_residual_scale: 1.0

# --- TCP geometry ---
tcp_radius: 0.10
tcp_filter_mode: chunk_center   # choices: chunk_center | chunk_end | state
tcp_target: [0.2818035953, 0.0385303870, 0.0602531905]
